<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.court.cipherapp">

    <application
        android:name=".ExampleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".caesarCipher" android:label="Caesar Cipher"/>
        <activity android:name=".substitutionCipher" android:label = "Substitution Cipher"
            android:windowSoftInputMode="adjustResize"
            />
        <activity android:name=".railCipher" android:label = "Transposition Cipher"/>
        <activity android:name=".vigenereCipher" android:label = "Vigenere Cipher"/>
        <activity android:name=".playFairCipher" android:label ="Playfair Cipher"></activity>

        // Delete this line later when you add fragments
        <activity android:name=".CaesarDecode" android:label = "Decode Caesar"></activity>
    </application>

</manifest>